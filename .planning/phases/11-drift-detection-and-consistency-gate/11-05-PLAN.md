---
phase: 11-drift-detection-and-consistency-gate
plan: 05
type: execute
wave: 3
depends_on: [11-04]
files_modified:
  - workflows/scan.md
  - workflows/audit-milestone.md
autonomous: true
requirements: [DRIFT-04, DRIFT-02]
nyquist_compliant: true

must_haves:
  truths:
    - "`/gsd:scan --drift` runs `gsd-tools verify drift` (no codebase-mapper agent) and prints a ranked top-N markdown drift report to stdout (DRIFT-04)"
    - "`/gsd:scan --drift --top N` limits the report; `--fail-on-score N` is passed through as the explicit hard-gate escalation"
    - "the report shows the suppressed (allowlisted) pairs as an auditable section, not silently hidden (D-07)"
    - "audit-milestone gains a §5.6 Drift Integrity Gate that is OFF by default (workflow.drift_gate absent/false = skip entirely) (D-05)"
    - "when enabled, the gate reports the composite score + findings with recommended-fix framing and NEVER blocks the milestone, unless workflow.drift_fail_on_score (or explicit --fail-on-score) is set and the score is below it (D-06)"
    - "the gate does not trip on the intentional CJS<->SDK dual resolver (the committed allowlist suppresses it — success criterion 1)"
  artifacts:
    - path: "workflows/scan.md"
      provides: "--drift non-agent branch (ranked markdown to stdout)"
      contains: "--drift"
    - path: "workflows/audit-milestone.md"
      provides: "§5.6 opt-in, warn-first Drift Integrity Gate"
      contains: "Drift Integrity Gate"
  key_links:
    - from: "workflows/scan.md --drift branch"
      to: "gsd-tools verify drift"
      via: "Bash: gsd-tools verify drift --scope . --top N [--fail-on-score N]"
      pattern: "verify drift"
    - from: "workflows/audit-milestone.md §5.6"
      to: "workflow.drift_gate config + verify drift"
      via: "config-get workflow.drift_gate gate then verify drift --json"
      pattern: "drift_gate|verify drift"
---

<objective>
Surface the `verify drift` subcommand (Plan 11-04) through its two consumer surfaces:
- `/gsd:scan --drift` — a non-agent, pure-compute branch that prints a ranked top-N markdown drift report to stdout (DRIFT-04; lighter than map-codebase, no mapper agent).
- `audit-milestone §5.6` — an opt-in, OFF-by-default, warn-first Drift Integrity Gate that reports score + findings with recommended-fix framing and never blocks unless an explicit `--fail-on-score`/config cutoff is set (DRIFT-02 / D-05 / D-06).

Both consume the JSON contract from 11-04, so this is Wave 3 (depends on 11-04). These are workflow-markdown edits only (no code), and they prove the phase's success criteria 1 and 2: the gate does not trip on the intentional dual resolver (committed allowlist), and `/gsd:scan --drift` produces a ranked report on gsd-plugin.

Purpose: DRIFT-04 (scan report surface), DRIFT-02 (opt-in warn-first release gate).
Output: the `--drift` branch in scan.md, the §5.6 gate in audit-milestone.md.
</objective>

<execution_context>
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/3.7.1/workflows/execute-plan.md
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/3.7.1/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-drift-detection-and-consistency-gate/11-CONTEXT.md
@.planning/phases/11-drift-detection-and-consistency-gate/11-RESEARCH.md
@.planning/phases/11-drift-detection-and-consistency-gate/11-PATTERNS.md
@workflows/scan.md
@workflows/audit-milestone.md

<interfaces>
<!-- The JSON contract from Plan 11-04 that both surfaces consume: -->

gsd-tools verify drift --scope . --json
  -> { skipped:false, score, findings:[{kind,file,line,severity,...}], suppressed:[{a,b,reason,...}], counts } | { skipped:true, reason }

Flags: --scope <dir>, --top <N> (limit ranked findings), --fail-on-score <N> (explicit hard gate -> exit 1 if score below cutoff; the ONLY non-zero path).

Config keys (added in 11-04):
  workflow.drift_gate            (bool, default OFF — gate skips entirely when absent/false; D-05)
  workflow.drift_fail_on_score   (number; when set, the gate escalates to a hard cutoff; D-06)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the --drift non-agent branch to scan.md (ranked markdown to stdout)</name>
  <files>workflows/scan.md</files>
  <read_first>
    - workflows/scan.md (full file — Step 1 arg-parse for --focus is the flag-handling shape; Steps 4-5 the mapper-agent path that --drift must BYPASS; the purpose/required_reading header)
    - .planning/phases/11-drift-detection-and-consistency-gate/11-PATTERNS.md (section "workflows/scan.md — --drift branch" lines 389-395; RESEARCH §Architecture Patterns Pattern 3)
    - .planning/phases/11-drift-detection-and-consistency-gate/11-RESEARCH.md (§Architecture Patterns Pattern 3 — parse --drift/--top/--fail-on-score, shell verify drift, print ranked markdown, NO mapper spawn)
  </read_first>
  <action>
    In `workflows/scan.md`, add a `--drift` mode handled at the TOP of Step 1 (argument parsing), BEFORE the focus resolution and BEFORE any mapper-agent spawn. When the user input contains `--drift`: this is a non-agent, pure-compute path — do NOT validate focus, do NOT spawn `gsd-codebase-mapper`, do NOT write to `.planning/codebase/`.

    Document the branch: parse `--top N` (default a sensible top-N like 20) and `--fail-on-score N` (optional pass-through). Run `gsd-tools verify drift --scope . --top {N} --json` (and append `--fail-on-score {N}` only when the user supplied it). Parse the JSON; if `skipped:true`, print a one-line "drift scan skipped: {reason}" and stop. Otherwise render a ranked markdown report to STDOUT with: a header (composite score), a ranked findings table (kind, file:line, severity), and a clearly-labeled "Suppressed (allowlisted) pairs" section listing each suppressed pair + its reason (D-07 auditability — never hide them). Keep it lighter than map-codebase: stdout only, no file artifacts.

    Add `--drift` (and its sub-flags) to the workflow's documented arguments so the skill advertises it. Make explicit in the prose that `--drift` is mutually exclusive with `--focus` (drift is its own mode). Commit: `feat(11-05): add /gsd:scan --drift ranked report branch`.
  </action>
  <verify>
    <automated>grep -q -- "--drift" workflows/scan.md && grep -q "verify drift" workflows/scan.md && grep -qi "suppress" workflows/scan.md</automated>
  </verify>
  <acceptance_criteria>
    - `workflows/scan.md` documents a `--drift` branch that runs `gsd-tools verify drift` and prints ranked markdown to stdout
    - The branch explicitly does NOT spawn the codebase-mapper agent and does NOT write `.planning/codebase/` files
    - The report includes a suppressed-pairs (allowlist) section (D-07 auditability)
    - `--top N` and pass-through `--fail-on-score N` are documented
    - Manual end-to-end: running the documented `gsd-tools verify drift --scope . --top 20 --json` command (the branch's core) returns valid JSON on the gsd-plugin repo (already proven in 11-04; this confirms the skill points at the working command)
  </acceptance_criteria>
  <done>scan.md has a working, documented --drift mode that surfaces the ranked report (with suppressed section) to stdout, no agent.</done>
</task>

<task type="auto">
  <name>Task 2: Add §5.6 Drift Integrity Gate to audit-milestone.md (opt-in, warn-first)</name>
  <files>workflows/audit-milestone.md</files>
  <read_first>
    - workflows/audit-milestone.md (lines 141-163 §5.5 Nyquist Compliance Discovery — the EXACT skeleton to mirror: config-flag read -> skip-if-disabled -> gather -> add-to-YAML -> "Discovery only / never auto-fails" note; lines 165-185 the audit YAML block where a `drift:` section is added)
    - .planning/phases/11-drift-detection-and-consistency-gate/11-PATTERNS.md (section "workflows/audit-milestone.md — new §5.6 opt-in gate" lines 363-385; RESEARCH §Architecture Patterns Pattern 2)
    - .planning/phases/11-drift-detection-and-consistency-gate/11-CONTEXT.md (D-05 opt-in/warn-first OFF-by-default; D-06 --fail-on-score explicit escalation only)
  </read_first>
  <action>
    In `workflows/audit-milestone.md`, add a new `## 5.6. Drift Integrity Gate (opt-in)` section immediately after §5.5, mirroring §5.5's skeleton exactly. Open with: "Skip if `workflow.drift_gate` is not `true` (absent/false = disabled, default OFF — D-05)." Show the config read: `DRIFT_GATE=$(gsd-sdk query config-get workflow.drift_gate --raw --default false)`. If not `true`: skip entirely.

    Otherwise: read the optional cutoff `DRIFT_FAIL=$(gsd-sdk query config-get workflow.drift_fail_on_score --raw --default "")`; run `gsd-tools verify drift --scope . --json` (append `--fail-on-score $DRIFT_FAIL` only when the cutoff is set — D-06). Parse the JSON (composite score, findings, suppressed). Report with RECOMMENDED-FIX framing (per Phase 10 CONVENTION-tier stance and the "recommended outcome, not rubber-stamp" principle): present findings as recommendations, surface the suppressed list so the dual-resolver suppression is auditable. Add a `drift:` block to the audit YAML: `drift: { score, findings_count, suppressed_count, overall }`.

    State the non-blocking contract explicitly (mirror §5.5's "Discovery only — never auto-calls" note): "Never blocks the milestone. The milestone fails ONLY when `workflow.drift_fail_on_score` (or an explicit `--fail-on-score N`) is set AND the score is below it (D-06) — the hard cutoff is never imposed by default." Confirm in the prose that with the committed `.gsd/drift-allowlist.json`, the intentional CJS<->SDK dual resolver is suppressed and does not contribute to the gate score (success criterion 1).

    Commit: `feat(11-05): add opt-in warn-first Drift Integrity Gate to audit-milestone`.
  </action>
  <verify>
    <automated>grep -q "Drift Integrity Gate" workflows/audit-milestone.md && grep -q "workflow.drift_gate" workflows/audit-milestone.md && grep -q "drift_fail_on_score" workflows/audit-milestone.md && grep -qi "never block\|never auto" workflows/audit-milestone.md</automated>
  </verify>
  <acceptance_criteria>
    - `workflows/audit-milestone.md` has a §5.6 Drift Integrity Gate after §5.5
    - The gate reads `workflow.drift_gate` and skips entirely when absent/false (default OFF — D-05)
    - When enabled it runs `verify drift --json`, reports score + findings + suppressed with recommended-fix framing, and adds a `drift:` block to the audit YAML
    - The prose states the gate never blocks unless `workflow.drift_fail_on_score` / explicit `--fail-on-score` is set and the score is below it (D-06)
    - The prose notes the committed allowlist suppresses the dual resolver so the gate does not trip on it (success criterion 1)
  </acceptance_criteria>
  <done>audit-milestone has an opt-in, OFF-by-default, warn-first drift gate that never blocks by default and surfaces the auditable suppressed list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user CLI flags -> scan --drift | `--top`/`--fail-on-score`/`--drift` cross from the shell into the skill; passed to verify drift which sanitizes |
| config store -> audit-milestone gate | `workflow.drift_gate` / `workflow.drift_fail_on_score` drive whether the gate runs and whether it can fail the milestone |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | Elevation of Privilege | the gate silently failing a milestone | mitigate | Default OFF (drift_gate absent/false skips); never blocks unless explicit drift_fail_on_score/--fail-on-score; the non-zero path lives only in the 11-04 handler, gated on the explicit flag (D-06) |
| T-11-10 | Information disclosure | suppressed/allowlisted pairs hidden from the report | mitigate | Both surfaces render the suppressed list as an auditable section (D-07) — suppression is visible, not silent |
| T-11-01 | Tampering | --scope/--top/--fail-on-score flag values | mitigate | Forwarded to `gsd-tools verify drift`, which sanitizes the scope path and coerces the numbers (11-04 / Phase 10 path-safety) |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | Workflow-markdown only; no packages installed; the underlying command is zero-dep |
</threat_model>

<verification>
- `/gsd:scan --drift` documented as a non-agent branch running `verify drift`, printing ranked markdown + suppressed section to stdout (DRIFT-04)
- audit-milestone §5.6 gate is OFF by default, warn-first, never-block-unless-explicit-cutoff (DRIFT-02 / D-05 / D-06)
- Both surfaces surface the suppressed/allowlisted pairs (D-07)
- End-to-end: `gsd-tools verify drift --scope . --json` on gsd-plugin returns valid JSON with the dual resolver in `suppressed` (success criteria 1 + 2)
</verification>

<success_criteria>
- `/gsd:scan --drift` produces a ranked top-N markdown drift report on gsd-plugin to stdout, no mapper agent (DRIFT-04 / success criterion 2)
- audit-milestone runs an optional, config-gated, warn-first integrity gate that the intentional CJS<->SDK dual resolver does not trip; suppressions auditable in the report (DRIFT-02 / success criterion 1)
- Hard cutoff only via explicit `--fail-on-score`/config; never imposed by default (D-06)
</success_criteria>

<output>
Create `.planning/phases/11-drift-detection-and-consistency-gate/11-05-SUMMARY.md` when done
</output>
