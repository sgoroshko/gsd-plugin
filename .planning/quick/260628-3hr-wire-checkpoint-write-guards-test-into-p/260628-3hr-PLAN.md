---
quick_id: 260628-3hr
slug: wire-checkpoint-write-guards-test-into-p
date: 2026-06-28
type: quick
---

<objective>
Make the issue #17 regression test (`tests/checkpoint-write-guards.test.cjs`, added by the debug fix in commit 79c5f63) a pre-release guarantee, so the checkpoint-hook-wipes-HANDOFF failure can never ship again. The test already exists and passes (8/8); this task wires it into the pre-release gate (CI on every push) and documents a pre-release checklist.
</objective>

<tasks>

<task>
  <name>Task 1: Run the checkpoint write-guards regression test in CI</name>
  <files>.github/workflows/check-drift.yml</files>
  <action>
    Add a step to the existing `handoff-schema` job (which already validates HANDOFF/checkpoint integrity) that runs `node tests/checkpoint-write-guards.test.cjs`. Name it consistently with the existing regression jobs ("... (regression for #17)"). CI runs on every push and pull request, so this gates the test before any tag/release.
  </action>
  <verify>YAML parses; the step references the real test file; `node tests/checkpoint-write-guards.test.cjs` exits 0.</verify>
</task>

<task>
  <name>Task 2: Document a pre-release checklist (RELEASING.md)</name>
  <files>RELEASING.md</files>
  <action>
    Create a minimal RELEASING.md with a pre-release checklist that points at the CI gate as the source of truth and lists the full node regression suite (including the #17 checkpoint write-guards test), plus the existing release steps (bump plugin.json + marketplace.json, CHANGELOG, tag, GitHub release). No em-dashes.
  </action>
  <verify>RELEASING.md exists and references the checkpoint write-guards test + the version-bump/tag steps.</verify>
</task>

</tasks>

<success_criteria>
- CI runs `tests/checkpoint-write-guards.test.cjs` on every push (pre-release enforcement)
- RELEASING.md documents the pre-release checklist
- The #17 failure mode cannot ship again without CI failing
</success_criteria>
