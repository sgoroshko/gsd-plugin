---
phase: 260515-f2d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/
  - hooks/gsd-context-monitor.js
  - hooks/gsd-workflow-guard.js
  - bin/lib/
  - bin/gsd-tools.cjs
  - sdk/src/
  - sdk/dist/cli.js
  - workflows/
  - templates/
  - references/
  - package.json
  - .claude-plugin/plugin.json
  - .claude-plugin/marketplace.json
  - README.md
  - .planning/PROJECT.md
  - CHANGELOG.md
  - .planning/STATE.md
autonomous: true
requirements: []
tags: [upstream-sync, plugin-patches, version-bump]

must_haves:
  truths:
    - "Plugin tree matches upstream get-shit-done v1.42.2 source-of-truth content for agents/, hooks/{gsd-context-monitor,gsd-workflow-guard}.js, bin/lib/*, sdk/src/*, workflows/, templates/, references/"
    - "The 4 plugin patches in upstream-synced files (#PLUGIN-AGENTS-DIR codename for the `[PLUGIN PATCH]` markers in core.cjs's getAgentsDir + resolveGsdRoot helpers, #PLUGIN-MODEL-CATALOG-PATH marker in model-catalog.cjs, #PLUGIN-HOOK-CONTEXT-MONITOR marker in gsd-context-monitor.js, plus 4 dispatch cases in gsd-tools.cjs) AND the 2 SDK source patches (`[PLUGIN PATCH]` markers in sdk/src/query/state-project-load.ts and sdk/src/query-gsd-tools-path.ts) survive the sync verbatim. Plugin-owned bin/gsd-sdk + bin/gsd-sdk.cmd wrappers (#PLUGIN-WRAPPER-ENV-EXPORT codename) remain byte-identical (sha256-verified), never touched by upstream sync."
    - "commands/ directory remains absent in the plugin tree (was not added by the sync)"
    - "bin/gsd-sdk and bin/gsd-sdk.cmd wrapper files are unchanged (plugin-owned, never synced from upstream)"
    - "sdk/dist/cli.js is freshly rebuilt and >= 1.4 MB, containing the SDK source patches (>= 2 CLAUDE_PLUGIN_ROOT matches, one per patched module)"
    - "Three metadata files (package.json, .claude-plugin/plugin.json, .claude-plugin/marketplace.json) all report version 2.43.0"
    - "README.md header references upstream GSD v1.42.2 as the new base (first 10 lines), and the stale v1.41.2 header reference is removed"
    - "CHANGELOG.md has a new [2.43.0] section dated 2026-05-15 with concise upstream highlights and the 'based on upstream GSD 1.42.2' marker"
    - "Existing regression tests (tests/mcp-stdio-framing.test.cjs, tests/workspace-json-integration.test.cjs, tests/hooks-smoke.test.cjs) still pass against the synced tree"
    - "STATE.md Quick Tasks Completed table has a new 260515-f2d row, last_activity bumped to 2026-05-15"
  artifacts:
    - path: .planning/quick/260515-f2d-new-upstream-version/260515-f2d-PLAN.md
      provides: "this plan file"
    - path: package.json
      provides: "plugin version metadata"
      contains: '"version": "2.43.0"'
    - path: .claude-plugin/plugin.json
      provides: "Claude Code plugin manifest"
      contains: '"version": "2.43.0"'
    - path: .claude-plugin/marketplace.json
      provides: "marketplace manifest (commonly missed in prior syncs)"
      contains: '"version": "2.43.0"'
    - path: sdk/dist/cli.js
      provides: "rebundled SDK CLI carrying SDK source patches"
      min_bytes: 1400000
    - path: CHANGELOG.md
      provides: "release notes entry"
      contains: "## [2.43.0] - 2026-05-15"
    - path: README.md
      provides: "user-facing version reference"
      contains: "v1.42.2"
    - path: .planning/STATE.md
      provides: "project state with quick task row"
      contains: "260515-f2d"
  key_links:
    - from: "bin/lib/core.cjs"
      to: "plugin-flat-layout agents/"
      via: "getAgentsDir() patched to prefer resolveGsdRoot() + 'agents'"
      pattern: "[PLUGIN PATCH] (codename #PLUGIN-AGENTS-DIR)"
    - from: "bin/lib/model-catalog.cjs"
      to: "sdk/shared/model-catalog.json"
      via: "flat-layout candidate prepended to resolver list"
      pattern: "#PLUGIN-MODEL-CATALOG-PATH"
    - from: "hooks/gsd-context-monitor.js"
      to: "bin/gsd-tools.cjs"
      via: "__dirname traversal with 'get-shit-done' segment dropped + GSD_TOOLS_PATH env override"
      pattern: "#PLUGIN-HOOK-CONTEXT-MONITOR"
    - from: "sdk/dist/cli.js"
      to: "sdk/src/query/state-project-load.ts and sdk/src/query-gsd-tools-path.ts"
      via: "rebundled by 'cd sdk && npm run build' after re-applying SDK source patches"
      pattern: "CLAUDE_PLUGIN_ROOT candidates inside compiled bundle (>= 2 matches)"
---

<objective>
Sync upstream get-shit-done v1.42.2 source tree into the plugin layout (currently at v1.41.2 baseline) and ship as plugin v2.43.0. This is the 7th upstream sync in this plugin's history and follows the established 6-commit pattern (commits 1-6 below). All 4 active plugin patches PLUS the 2 SDK source patches must be re-applied surgically; commands/ must NOT be copied; the SDK bundle must be rebuilt after SDK source patches are restored.

Purpose: Keep the plugin current with upstream while preserving plugin-specific patches and architecture.

Output: 5 or 6 atomic commits on the worktree branch, totalling a clean working tree at v2.43.0 ready for orchestrator merge + release.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jnuyens/src/gsd-plugin/.planning/STATE.md
@/Users/jnuyens/src/gsd-plugin/CHANGELOG.md
@/Users/jnuyens/.claude/projects/-Users-jnuyens-src-gsd-plugin/memory/feedback_plugin_patches_inventory.md
@/Users/jnuyens/src/gsd-plugin/.planning/quick/260513-2zg-audit-upstream-hooks-tree-and-pull-in-se/260513-2zg-SUMMARY.md

# Source-of-truth upstream tree (read-only, read via `git -C /tmp/gsd-upstream-check show v1.42.2:<path>`)
- /tmp/gsd-upstream-check/  — fresh clone, checked out at v1.42.2
- /tmp/gsd-upstream-check/CHANGELOG.md  — upstream changelog for v1.41.2 -> v1.42.0 -> v1.42.1 -> v1.42.2

<interfaces>
<!-- Plugin patch surfaces — extracted from the live plugin tree at v2.42.6 -->
<!-- Executor: re-apply these EXACTLY after wholesale upstream copy. Do not paraphrase markers. -->

CODENAME vs LITERAL CLARIFICATION:
  - "#PLUGIN-AGENTS-DIR", "#PLUGIN-MODEL-CATALOG-PATH", "#PLUGIN-HOOK-CONTEXT-MONITOR", "#PLUGIN-WRAPPER-ENV-EXPORT" are CODENAMES used in the memory-inventory feedback file. They are NOT all present as literal in-source strings.
  - The actual in-source markers are:
      * bin/lib/core.cjs : two `[PLUGIN PATCH]` comment blocks (HEAD lines 21 and 1288). Functional landmarks: `resolveGsdRoot` exports + `function getAgentsDir` body.
      * bin/lib/model-catalog.cjs : literal `[PLUGIN PATCH #PLUGIN-MODEL-CATALOG-PATH]` at line 8.
      * hooks/gsd-context-monitor.js : literal `[PLUGIN PATCH] #PLUGIN-HOOK-CONTEXT-MONITOR` at line 139.
      * sdk/src/query/state-project-load.ts : literal `[PLUGIN PATCH]` at line 27.
      * sdk/src/query-gsd-tools-path.ts : literal `[PLUGIN PATCH]` at line 10.
      * bin/gsd-tools.cjs : NO literal marker; identified by 4 dispatch cases.
  - Grep gates therefore must target the actual in-source string per file, not the codename.

Patch 1: bin/lib/core.cjs ([PLUGIN PATCH] -- codename #PLUGIN-AGENTS-DIR)
  - Added exports: resolveGsdRoot, resolveGsdDataDir, resolveGsdAsset
  - Patched function: getAgentsDir()
    - Behaviour: prefer path.join(resolveGsdRoot(), 'agents') when that dir exists,
      before falling back to upstream's path.join(__dirname, '..', '..', '..', 'agents')
  - In-source markers: two `[PLUGIN PATCH]` comment blocks (HEAD lines 21 and 1288); functional landmarks `resolveGsdRoot` (export) and `function getAgentsDir` (the patched body).
  - Upstream v1.41.2 -> v1.42.2 diff: 486 lines (substantial). DO NOT cherry-pick lines; copy the upstream file wholesale, then re-insert the helpers + getAgentsDir patch via Edit.
  - Source of truth for the patch shape: `git -C "$WT_ROOT" show HEAD:bin/lib/core.cjs` (pre-copy plugin HEAD).

Patch 2: bin/lib/model-catalog.cjs ([PLUGIN PATCH #PLUGIN-MODEL-CATALOG-PATH])
  - Upstream uses a 3-candidate resolver (install-path -> source-repo dev path -> GSD_MODEL_CATALOG env)
  - Patch prepends path.resolve(__dirname, '..', '..', 'sdk', 'shared', 'model-catalog.json') as candidate #0
  - In-source marker: literal `[PLUGIN PATCH #PLUGIN-MODEL-CATALOG-PATH]` in the file (grep gate).
  - Upstream v1.41.2 -> v1.42.2 diff: 2 lines only. Confirm the upstream resolver shape unchanged. If it is, fold the plugin candidate into the new candidate list at index 0 same as v2.42.4.

Patch 3: bin/gsd-tools.cjs -- 4 dispatch cases (NO literal marker, but enumerable):
  - case 'write-phase-memory': -> lib/memory.cjs::cmdWritePhaseMemory
  - case 'checkpoint': -> lib/checkpoint.cjs::cmdCheckpoint
  - case 'hook': with subtypes 'session-start' / 'pre-compact' / 'post-tool-use' / 'stop'
  - case 'migrate': -> migrations/legacy-cleanup.cjs
  - Upstream v1.41.2 -> v1.42.2 diff: 143 lines. Read `git show HEAD:bin/gsd-tools.cjs` BEFORE copy to capture the exact case blocks; re-insert into the upstream wholesale-copied file at the matching switch.

Patch 4: hooks/gsd-context-monitor.js ([PLUGIN PATCH] #PLUGIN-HOOK-CONTEXT-MONITOR)
  - Upstream resolves: path.join(__dirname, '..', 'get-shit-done', 'bin', 'gsd-tools.cjs')
  - Plugin patch drops the 'get-shit-done' segment: path.join(__dirname, '..', 'bin', 'gsd-tools.cjs')
  - Plus GSD_TOOLS_PATH env override
  - In-source marker: literal `[PLUGIN PATCH] #PLUGIN-HOOK-CONTEXT-MONITOR` at line 139 (grep gate).
  - Upstream v1.41.2 -> v1.42.2 diff: 4 lines. Likely trivial; surgical re-apply.

Patch 5 (CRITICAL, EASY TO MISS): SDK source patches (sdk/src/) -- two files:
  - sdk/src/query/state-project-load.ts -- CLAUDE_PLUGIN_ROOT candidate, in-source marker `[PLUGIN PATCH]` at line 27.
  - sdk/src/query-gsd-tools-path.ts -- CLAUDE_PLUGIN_ROOT candidate, in-source marker `[PLUGIN PATCH]` at line 10.
  - These are 2 SMALL inline blocks INSIDE a 50+ file wholesale sdk/src/* copy. They are EASY to miss.
  - DISCIPLINE: re-apply IMMEDIATELY after the wholesale copy and BEFORE rebundle.
  - After re-applying both, rebuild bundle: `cd sdk && npm run build`
  - Output: sdk/dist/cli.js (>= 1.4 MB; safety-net gate: `grep -c CLAUDE_PLUGIN_ROOT sdk/dist/cli.js` >= 2 -- one match per patched module).

Plugin-OWNED files (DO NOT touch during wholesale copy):
  - bin/gsd-sdk (#PLUGIN-WRAPPER-ENV-EXPORT codename -- POSIX wrapper)
  - bin/gsd-sdk.cmd (Windows wrapper)
  - bin/maintenance/check-drift.cjs
  - bin/validate-plugin.cjs
  - hooks/gsd-prompt-guard.js, gsd-read-guard.js, gsd-read-injection-scanner.js, gsd-validate-commit.sh, gsd-phase-boundary.sh, gsd-session-state.sh, hooks.json, hooks/lib/
    (Upstream did not change these between v1.41.2..v1.42.2 -- verified by `git diff --name-only v1.41.2..v1.42.2 -- hooks/` which lists only gsd-context-monitor.js, gsd-workflow-guard.js, gsd-statusline.js, gsd-update-banner.js. Plugin does NOT have the latter two; they are out of scope.)
  - tests/  -- plugin-owned regression tests
  - .planning/  -- plugin-owned planning artifacts
  - .claude-plugin/  -- plugin manifest dir
  - README.md, CHANGELOG.md, CONTEXT.md, CONTRIBUTING.md, VERSIONING.md, LICENSE -- plugin-owned docs
</interfaces>

<worktree_branch_check>
Before EVERY commit, the executor MUST assert:
1. cwd is the worktree (not main repo). All file edits use absolute paths under the worktree root.
2. `git -C "$WT" rev-parse --abbrev-ref HEAD` returns the quick-task branch (NOT master).
3. `git -C "$WT" status` shows the expected staged files (no surprise additions, no missing patch markers).
4. After committing, `git -C "$WT" log -1 --stat` confirms only the intended files changed.

If any check fails, STOP and surface as deviation. The worktree HEAD-assertion hook (#2924) will refuse any accidental commit to a protected ref. Recover by moving files into the worktree before retry.
</worktree_branch_check>

<assumed_paths>
- WT_ROOT: the worktree path (resolve via `git worktree list` at task start; do NOT hardcode)
- UPSTREAM: /tmp/gsd-upstream-check  (v1.42.2 source-of-truth, read-only)
- In-source markers to grep AFTER each commit touching the relevant file:
  - bin/lib/core.cjs : `grep -c '\[PLUGIN PATCH\]' bin/lib/core.cjs` >= 2 AND `grep -q 'resolveGsdRoot' bin/lib/core.cjs` AND `grep -q 'function getAgentsDir' bin/lib/core.cjs`
  - bin/lib/model-catalog.cjs : `grep -q '#PLUGIN-MODEL-CATALOG-PATH' bin/lib/model-catalog.cjs`
  - hooks/gsd-context-monitor.js : `grep -q '#PLUGIN-HOOK-CONTEXT-MONITOR' hooks/gsd-context-monitor.js`
  - bin/gsd-tools.cjs : "case 'write-phase-memory'" AND "case 'checkpoint'" AND "case 'hook'" AND "case 'migrate'"
  - sdk/src/query/state-project-load.ts : `grep -q '\[PLUGIN PATCH\]' sdk/src/query/state-project-load.ts`
  - sdk/src/query-gsd-tools-path.ts : `grep -q '\[PLUGIN PATCH\]' sdk/src/query-gsd-tools-path.ts`
  - sdk/dist/cli.js : `grep -c CLAUDE_PLUGIN_ROOT sdk/dist/cli.js` >= 2 (proves rebundle carried BOTH SDK source patches)
- bin/gsd-sdk and bin/gsd-sdk.cmd MUST be byte-identical post-task-1 (sha256 unchanged from pre-task-1). Sample the hashes at task-1 start to compare at task-1 end.
</assumed_paths>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wholesale upstream source-tree copy + re-apply 4 plugin patches + re-apply 2 SDK source patches + rebundle sdk/dist/cli.js</name>
  <files>
    agents/ (28 files refreshed wholesale from upstream),
    hooks/gsd-context-monitor.js (refresh + re-apply [PLUGIN PATCH] #PLUGIN-HOOK-CONTEXT-MONITOR),
    hooks/gsd-workflow-guard.js (refresh wholesale, no patch),
    bin/lib/core.cjs (refresh + re-apply two [PLUGIN PATCH] blocks: resolveGsdRoot helpers and getAgentsDir body, codename #PLUGIN-AGENTS-DIR),
    bin/lib/model-catalog.cjs (refresh + re-apply [PLUGIN PATCH #PLUGIN-MODEL-CATALOG-PATH]),
    bin/lib/*.cjs (refresh remaining 48 lib files wholesale),
    bin/gsd-tools.cjs (refresh + re-insert 4 plugin dispatch cases),
    sdk/src/* (refresh wholesale, then re-apply [PLUGIN PATCH] markers in state-project-load.ts and query-gsd-tools-path.ts -- EASY TO MISS, see callout),
    sdk/dist/cli.js (rebuilt via `cd sdk && npm run build`),
    workflows/* (refresh from upstream get-shit-done/workflows/),
    templates/* (refresh from upstream get-shit-done/templates/),
    references/* (refresh from upstream get-shit-done/references/)
  </files>
  <action>
    Operate on the worktree only (cd to WT_ROOT, use absolute paths). Before any modification, snapshot sha256 of bin/gsd-sdk and bin/gsd-sdk.cmd. These MUST be unchanged at end of task (plugin-owned wrappers, NOT upstream-synced).

    Pre-Task-1 SDK source path audit (MANDATORY before wholesale sdk/src/* copy):
      Run: `git -C /tmp/gsd-upstream-check diff --name-status v1.41.2..v1.42.2 -- sdk/src/ > /tmp/sdk-src-diff.txt`
      Read /tmp/sdk-src-diff.txt and assert that BOTH `sdk/src/query/state-project-load.ts` AND `sdk/src/query-gsd-tools-path.ts` appear as either:
        (a) Modified (M) in the diff, OR
        (b) Unchanged (absent from the diff entirely).
      If EITHER patched file appears as Renamed (R), Copied (C), or Deleted (D) in /tmp/sdk-src-diff.txt: ABORT Task 1 and surface the deviation "SDK source patch paths moved upstream -- manual patch port required" to the user. Do NOT proceed with the wholesale copy until the user confirms how to map the renamed/moved paths.

    Source-tree wholesale copy from /tmp/gsd-upstream-check (at v1.42.2). Map upstream paths -> plugin paths exactly as per the established v2.42.4 / v2.42.6 pattern:
      - upstream/agents/ -> WT_ROOT/agents/
      - upstream/hooks/gsd-context-monitor.js -> WT_ROOT/hooks/gsd-context-monitor.js (overwrite)
      - upstream/hooks/gsd-workflow-guard.js -> WT_ROOT/hooks/gsd-workflow-guard.js (overwrite)
      - upstream/get-shit-done/bin/lib/*.cjs -> WT_ROOT/bin/lib/ (overwrite all)
      - upstream/get-shit-done/bin/gsd-tools.cjs -> WT_ROOT/bin/gsd-tools.cjs (overwrite)
      - upstream/sdk/src/* -> WT_ROOT/sdk/src/ (overwrite tree)
      - upstream/get-shit-done/workflows/*.md -> WT_ROOT/workflows/ (overwrite)
      - upstream/get-shit-done/templates/* -> WT_ROOT/templates/ (overwrite)
      - upstream/get-shit-done/references/* -> WT_ROOT/references/ (overwrite)

    EXCLUDED -- do NOT copy any of these even if upstream has them:
      - upstream/commands/  (HARD RULE -- see memory entry "No bundled commands/")
      - upstream/hooks/gsd-statusline.js  (plugin does NOT ship this hook)
      - upstream/hooks/gsd-update-banner.js  (plugin does NOT ship this hook)
      - any upstream file mapping to bin/gsd-sdk, bin/gsd-sdk.cmd, bin/maintenance/, bin/validate-plugin.cjs (plugin-owned)
      - hooks/lib/, hooks/gsd-prompt-guard.js, hooks/gsd-read-guard.js, hooks/gsd-read-injection-scanner.js, hooks/gsd-validate-commit.sh, hooks/gsd-phase-boundary.sh, hooks/gsd-session-state.sh -- upstream did NOT change these between v1.41.2..v1.42.2 (verified via `git -C /tmp/gsd-upstream-check diff --name-only v1.41.2..v1.42.2 -- hooks/`). Re-copying would be a no-op; skip to minimize diff.

    Re-apply the 4 active plugin patches after copy:
      1. bin/lib/core.cjs (codename #PLUGIN-AGENTS-DIR) -- Preserve the two `[PLUGIN PATCH]` comment blocks (currently at lines 21 and 1288 of HEAD bin/lib/core.cjs) along with the resolveGsdRoot/resolveGsdDataDir/resolveGsdAsset helper exports + the patched getAgentsDir body. Use `git show HEAD:bin/lib/core.cjs` as the source of truth for the patch shape. Surgically re-insert into the just-copied upstream file via Edit.
      2. bin/lib/model-catalog.cjs ([PLUGIN PATCH #PLUGIN-MODEL-CATALOG-PATH]) -- Confirm upstream's resolver shape is still the 3-candidate list (it is: only 2-line upstream diff). Prepend `path.resolve(__dirname, '..', '..', 'sdk', 'shared', 'model-catalog.json')` as candidate #0, same as v2.42.4. Preserve the literal in-source string `[PLUGIN PATCH #PLUGIN-MODEL-CATALOG-PATH]`.
      3. bin/gsd-tools.cjs -- Read pre-copy version (`git show HEAD:bin/gsd-tools.cjs`). Re-insert the 4 case blocks (write-phase-memory / checkpoint / hook / migrate) into the corresponding switch in the upstream-copied file. The hook case has subtypes 'session-start' / 'pre-compact' / 'post-tool-use' / 'stop'.
      4. hooks/gsd-context-monitor.js ([PLUGIN PATCH] #PLUGIN-HOOK-CONTEXT-MONITOR) -- Drop the 'get-shit-done' segment from the __dirname traversal that resolves gsd-tools.cjs; add GSD_TOOLS_PATH env override. Preserve the literal in-source string `[PLUGIN PATCH] #PLUGIN-HOOK-CONTEXT-MONITOR`.

    >>> CRITICAL CALLOUT: SDK source patches + rebuild <<<
      The 2 SDK source patches below are SMALL inline blocks INSIDE a 50+ file wholesale sdk/src/* copy that you just performed. They are EASY TO FORGET. If you skip them, the rebundle in step 6 will silently produce a bundle WITHOUT the patches, the post-rebundle gate may still pass (depending on how loose it is), and the agents_installed regression that this plugin has fought multiple times before will return.
      DISCIPLINE: re-apply the SDK source patches IMMEDIATELY now, BEFORE running step 6 (rebundle). The post-rebundle gate (>= 2 CLAUDE_PLUGIN_ROOT matches in sdk/dist/cli.js) is the SAFETY NET, not the primary discipline.

      5. sdk/src/query/state-project-load.ts and sdk/src/query-gsd-tools-path.ts -- re-apply the CLAUDE_PLUGIN_ROOT candidate from pre-copy state (`git show HEAD:sdk/src/query/state-project-load.ts` and `git show HEAD:sdk/src/query-gsd-tools-path.ts`). Preserve the literal `[PLUGIN PATCH]` in-source markers at line 27 and line 10 respectively.
      6. cd WT_ROOT/sdk && npm run build. Confirm sdk/dist/cli.js produced, file size >= 1.4 MB, and `grep -c CLAUDE_PLUGIN_ROOT sdk/dist/cli.js` >= 2 (one match per patched module). If the bundle is minified and the >= 2 grep is unreliable, fall back to: `grep -c 'state-project-load' sdk/dist/cli.js` >= 1 AND `grep -c 'query-gsd-tools-path' sdk/dist/cli.js` >= 1 to confirm both modules are present in the bundle.

    Run regression tests (from WT_ROOT) BEFORE committing:
      - node tests/mcp-stdio-framing.test.cjs
      - node tests/workspace-json-integration.test.cjs
      - node tests/hooks-smoke.test.cjs
    All three MUST pass. Surface failures as deviations and STOP -- do not commit a regressed tree.

    Pre-commit grep gates (executor MUST verify all of these BEFORE committing):
      - `grep -c '\[PLUGIN PATCH\]' bin/lib/core.cjs` >= 2  (the two patch comment blocks)
      - `grep -q 'resolveGsdRoot' bin/lib/core.cjs && grep -q 'function getAgentsDir' bin/lib/core.cjs`  (functional landmarks)
      - `grep -q '#PLUGIN-MODEL-CATALOG-PATH' bin/lib/model-catalog.cjs`
      - `grep -q '#PLUGIN-HOOK-CONTEXT-MONITOR' hooks/gsd-context-monitor.js`
      - `grep -c "case 'write-phase-memory'" bin/gsd-tools.cjs` >= 1
      - `grep -c "case 'checkpoint'" bin/gsd-tools.cjs` >= 1
      - `grep -c "case 'hook'" bin/gsd-tools.cjs` >= 1
      - `grep -c "case 'migrate'" bin/gsd-tools.cjs` >= 1
      - `grep -q '\[PLUGIN PATCH\]' sdk/src/query/state-project-load.ts`
      - `grep -q '\[PLUGIN PATCH\]' sdk/src/query-gsd-tools-path.ts`
      - sha256 of bin/gsd-sdk unchanged vs task-start snapshot
      - sha256 of bin/gsd-sdk.cmd unchanged vs task-start snapshot
      - `test ! -d commands`  (plugin must still have NO commands/ dir)
      - sdk/dist/cli.js size in bytes >= 1400000
      - `grep -c CLAUDE_PLUGIN_ROOT sdk/dist/cli.js` >= 2  (proves BOTH SDK source patches survived the rebundle)

    Commit (after worktree_branch_check assertions):
      git -C "$WT_ROOT" add agents hooks/gsd-context-monitor.js hooks/gsd-workflow-guard.js bin/lib bin/gsd-tools.cjs sdk/src sdk/dist/cli.js workflows templates references
      git -C "$WT_ROOT" commit -m "chore(260515-f2d): sync upstream get-shit-done v1.42.2 source tree

       - Wholesale refresh: agents/, hooks/{gsd-context-monitor,gsd-workflow-guard}.js,
         bin/lib/*, bin/gsd-tools.cjs, sdk/src/*, workflows/, templates/, references/
       - Re-applied 4 plugin patches in upstream-synced files: [PLUGIN PATCH] blocks
         in bin/lib/core.cjs (codename #PLUGIN-AGENTS-DIR),
         [PLUGIN PATCH #PLUGIN-MODEL-CATALOG-PATH] in bin/lib/model-catalog.cjs,
         [PLUGIN PATCH] #PLUGIN-HOOK-CONTEXT-MONITOR in hooks/gsd-context-monitor.js,
         plus 4 dispatch cases in bin/gsd-tools.cjs (write-phase-memory, checkpoint,
         hook, migrate)
       - Re-applied 2 SDK source [PLUGIN PATCH] blocks: sdk/src/query/state-project-load.ts
         and sdk/src/query-gsd-tools-path.ts. Rebundled sdk/dist/cli.js via
         'cd sdk && npm run build'; bundle contains >= 2 CLAUDE_PLUGIN_ROOT matches.
       - Excluded: commands/ (per policy), hooks/gsd-{statusline,update-banner}.js (not shipped)
       - Regression tests pass: mcp-stdio-framing, workspace-json-integration, hooks-smoke"

    DO NOT use Co-Authored-By lines in any commit message (per global instruction).
  </action>
  <verify>
    <automated>cd "$WT_ROOT" && node tests/mcp-stdio-framing.test.cjs && node tests/workspace-json-integration.test.cjs && node tests/hooks-smoke.test.cjs && [ $(grep -c '\[PLUGIN PATCH\]' bin/lib/core.cjs) -ge 2 ] && grep -q 'resolveGsdRoot' bin/lib/core.cjs && grep -q 'function getAgentsDir' bin/lib/core.cjs && grep -q '#PLUGIN-MODEL-CATALOG-PATH' bin/lib/model-catalog.cjs && grep -q '#PLUGIN-HOOK-CONTEXT-MONITOR' hooks/gsd-context-monitor.js && grep -q "case 'write-phase-memory'" bin/gsd-tools.cjs && grep -q "case 'migrate'" bin/gsd-tools.cjs && grep -q '\[PLUGIN PATCH\]' sdk/src/query/state-project-load.ts && grep -q '\[PLUGIN PATCH\]' sdk/src/query-gsd-tools-path.ts && [ ! -d commands ] && [ $(stat -f%z sdk/dist/cli.js 2>/dev/null || stat -c%s sdk/dist/cli.js) -ge 1400000 ] && [ $(grep -c CLAUDE_PLUGIN_ROOT sdk/dist/cli.js) -ge 2 ]</automated>
  </verify>
  <done>
    Upstream v1.42.2 source files copied into the plugin layout, all 4 plugin patches re-applied with grep-able in-source markers present, 2 SDK source patches re-applied with `[PLUGIN PATCH]` markers present, SDK bundle rebuilt and >= 1.4 MB with >= 2 CLAUDE_PLUGIN_ROOT matches, commands/ absent, plugin-owned wrappers untouched (sha256-verified), 3 regression test suites pass, single atomic commit created on the worktree branch.
  </done>
</task>

<task type="auto">
  <name>Task 2: Bump version 2.42.6 -> 2.43.0 across all 3 metadata files</name>
  <files>
    package.json,
    .claude-plugin/plugin.json,
    .claude-plugin/marketplace.json
  </files>
  <action>
    Update the "version" field in each of the THREE metadata files from "2.42.6" to "2.43.0":
      - package.json (top-level "version")
      - .claude-plugin/plugin.json (top-level "version")
      - .claude-plugin/marketplace.json (per-plugin "version" inside plugins[] array -- the entry where "name": "gsd")

    CRITICAL: marketplace.json was missed in v2.42.6 and several prior syncs. It is NOT optional. After the edit, verify with grep that all three files now show 2.43.0.

    Each file MUST still parse as valid JSON after the edit (no trailing commas introduced, no escape issues).

    Pre-commit grep gates:
      - `grep -c '"version": "2.43.0"' package.json` >= 1
      - `grep -c '"version": "2.43.0"' .claude-plugin/plugin.json` >= 1
      - `grep -c '"version": "2.43.0"' .claude-plugin/marketplace.json` >= 1
      - `grep -c '"version": "2.42.6"'` against all three files == 0 (no stale references)
      - `node -e 'JSON.parse(require("fs").readFileSync("package.json"))'` returns clean (no parse error)
      - same JSON-parse check for .claude-plugin/plugin.json and .claude-plugin/marketplace.json

    Commit (after worktree_branch_check):
      git -C "$WT_ROOT" add package.json .claude-plugin/plugin.json .claude-plugin/marketplace.json
      git -C "$WT_ROOT" commit -m "chore(260515-f2d): bump plugin version 2.42.6 -> 2.43.0 (upstream GSD 1.42.2)"
  </action>
  <verify>
    <automated>cd "$WT_ROOT" && [ $(grep -c '"version": "2.43.0"' package.json .claude-plugin/plugin.json .claude-plugin/marketplace.json | awk -F: '{s+=$2} END{print s}') -ge 3 ] && [ $(grep -c '"version": "2.42.6"' package.json .claude-plugin/plugin.json .claude-plugin/marketplace.json | awk -F: '{s+=$2} END{print s}') -eq 0 ] && node -e 'JSON.parse(require("fs").readFileSync("package.json")); JSON.parse(require("fs").readFileSync(".claude-plugin/plugin.json")); JSON.parse(require("fs").readFileSync(".claude-plugin/marketplace.json"))'</automated>
  </verify>
  <done>
    All three metadata files (package.json, .claude-plugin/plugin.json, .claude-plugin/marketplace.json) show "version": "2.43.0", no remaining references to 2.42.6 across the three files, all JSON valid, single atomic commit.
  </done>
</task>

<task type="auto">
  <name>Task 3: README.md + .planning/PROJECT.md upstream-reference refresh</name>
  <files>
    README.md,
    .planning/PROJECT.md (only if it carries the upstream version string)
  </files>
  <action>
    Read README.md first 10 lines BEFORE editing -- the current header (HEAD) is:
      Line 3: "**Based on:** [GSD 1.41.2](https://github.com/gsd-build/get-shit-done/releases/tag/v1.41.2) base tree by **TACHES** (Lex Christopherson)"
      Line 5: "**Plugin version:** `2.42.6`"

    Update README.md header text:
      - Line 3: change "GSD 1.41.2" -> "GSD 1.42.2" AND `tag/v1.41.2` -> `tag/v1.42.2` in the URL.
      - Line 5: change `2.42.6` -> `2.43.0`.
      - Search the rest of README.md for other forward-facing references to "v1.41.2", "1.41.2", "2.42.6" and update to v1.42.2 / 2.43.0 where the reference is to the upstream GSD base version or the current plugin version (install snippets, version-notes blocks).
      - Do NOT touch historical CHANGELOG-style mentions of older versions inside README (e.g. inside a "## Version history" table). Only forward-facing "current version" references.

    Update .planning/PROJECT.md ONLY if it has a current upstream version string. Read it first; if the string isn't present, skip the file.

    Pre-commit gates (HEADER-SCOPED, positive + negative):
      - `grep -q 'Based on:.*GSD 1.42.2' <(head -10 README.md)`  (positive: new header reference present in first 10 lines)
      - `! grep -q 'GSD 1.41.2' <(head -10 README.md)`  (negative: stale v1.41.2 reference removed from first 10 lines)
      - `grep -q '2.43.0' <(head -10 README.md)`  (positive: plugin version updated in header)
      - `! grep -q '2.42.6' <(head -10 README.md)`  (negative: stale plugin version removed from header)
      - Em-dash diff-scoped gate: `git -C "$WT_ROOT" diff --cached README.md | awk '/^\+/ && !/^\+\+\+/' | grep -c '—'` == 0  (no em-dashes introduced in this diff)

    Commit (after worktree_branch_check):
      git -C "$WT_ROOT" add README.md
      # if PROJECT.md modified, include it
      git -C "$WT_ROOT" commit -m "docs(260515-f2d): refresh upstream reference to GSD v1.42.2 + plugin v2.43.0"
  </action>
  <verify>
    <automated>cd "$WT_ROOT" && grep -q 'Based on:.*GSD 1.42.2' <(head -10 README.md) && ! grep -q 'GSD 1.41.2' <(head -10 README.md) && grep -q '2.43.0' <(head -10 README.md) && ! grep -q '2.42.6' <(head -10 README.md)</automated>
  </verify>
  <done>
    README.md header (first 10 lines) references upstream GSD 1.42.2 and plugin v2.43.0; stale v1.41.2 and 2.42.6 references removed from the header; PROJECT.md updated if it carried the upstream version string; no em-dashes introduced in the diff; single atomic commit.
  </done>
</task>

<task type="auto">
  <name>Task 4: CHANGELOG.md -- add [2.43.0] entry with concise upstream highlights</name>
  <files>CHANGELOG.md</files>
  <action>
    Prepend a new "## [2.43.0] - 2026-05-15  (based on upstream GSD 1.42.2)" section between the existing "## [Unreleased]" marker and "## [2.42.6]".

    Format MUST match the established CHANGELOG style (see [2.42.4] and [2.42.6] entries as templates). Sections to include:
      - One-paragraph summary (2-4 sentences) explaining this is an upstream patch sync from v1.41.2 -> v1.42.2 (which includes the intermediate v1.42.0 and v1.42.1 GA releases that the plugin skipped via daily-sync cadence change).
      - ### Changed  -- list "Version bump 2.42.6 -> 2.43.0" and "bin/lib/{core,model-catalog}.cjs + bin/gsd-tools.cjs + agents/ + hooks/{gsd-context-monitor,gsd-workflow-guard}.js + sdk/src/* + workflows/ + templates/ + references/ refreshed wholesale from upstream"
      - ### Fixed  -- bullet list of 3-5 most plugin-relevant upstream fixes. Examples (executor: select from the headline_upstream_changes list, do NOT enumerate all):
        - State Document Module via generator (v1.42.2, #3531) -- Phase 1 of #3524
        - ROADMAP regex consolidation (v1.42.2, #3537)
        - Atomic writes in scripts/build-hooks.js (flaky CI fix)
        - SKILL.md description quoting fixes (Copilot/Antigravity/Trae/CodeBuddy)
        - config-get context_window default 200000 when unset
        - milestone-archive layout support in validate consistency/health/find-phase
      - ### Added (if relevant)  -- list the 1-2 most plugin-relevant new features. Examples:
        - Six namespace meta-skills (gsd:workflow, gsd:project, gsd:review, gsd:context, gsd:manage, gsd:ideate) -- two-stage hierarchical routing (#2792)
        - /gsd-health --context utilization guard (#2792)
        - --minimal install flag (#2762)
        - Phase-lifecycle status-line read-side (#2833)
        - /gsd-edit-phase command (#2617)
        - Workstream config inheritance (#2714)
      - ### Plugin patches -- preserved verbatim  -- list the 4 in-tree patches (codenames #PLUGIN-AGENTS-DIR, #PLUGIN-MODEL-CATALOG-PATH, #PLUGIN-HOOK-CONTEXT-MONITOR, plus the 4 dispatch cases in bin/gsd-tools.cjs) AND the 2 SDK source patches (sdk/src/query/state-project-load.ts + sdk/src/query-gsd-tools-path.ts), each one line, noting whether upstream changed the file and how the patch was re-applied (surgical / clean / evolved).
      - Link line: "See full upstream release notes: <https://github.com/gsd-build/get-shit-done/releases/tag/v1.42.2>"

    Notes -- keep it CONCISE. The full upstream changelog has ~50 entries spanning 4 patch releases (v1.42.0 + v1.42.1 + v1.42.2 + ongoing v1.42.x). Do NOT enumerate all of them. Pick 3-5 fixes and 1-2 features that genuinely matter for plugin users. Reference the upstream link for completeness.

    Style rules:
      - No em-dashes (`—` U+2014) introduced. Use commas, colons, or parentheses instead. (Per memory entry "No em-dashes in docs".)
      - Use upstream PR / issue numbers in (#NNNN) form for traceability.

    Pre-commit grep gates:
      - `grep -c '## \[2.43.0\] - 2026-05-15' CHANGELOG.md` == 1
      - `grep -c 'based on upstream GSD 1.42.2' CHANGELOG.md` >= 1
      - Em-dash diff-scoped gate: `git -C "$WT_ROOT" diff --cached CHANGELOG.md | awk '/^\+/ && !/^\+\+\+/' | grep -c '—'` == 0  (no em-dashes introduced in this diff; existing older entries may have em-dashes from earlier ungated commits and are unaffected by this scoped gate)
      - The new section appears BEFORE "## [2.42.6]" in the file.

    Commit (after worktree_branch_check):
      git -C "$WT_ROOT" add CHANGELOG.md
      git -C "$WT_ROOT" commit -m "docs(260515-f2d): CHANGELOG entry for v2.43.0 (upstream GSD 1.42.2)"
  </action>
  <verify>
    <automated>cd "$WT_ROOT" && grep -q '## \[2.43.0\] - 2026-05-15' CHANGELOG.md && grep -q 'based on upstream GSD 1.42.2' CHANGELOG.md && awk '/^## \[2.43.0\]/{found=1} /^## \[2.42.6\]/{if(found){print "OK"; exit 0} else {print "ORDER_WRONG"; exit 1}}' CHANGELOG.md | grep -q OK</automated>
  </verify>
  <done>
    CHANGELOG.md has a new [2.43.0] - 2026-05-15 section, properly ordered above [2.42.6], formatted to match prior entries (concise, no em-dashes introduced in the diff, 3-5 fixes + 1-2 features + 4 in-tree patches + 2 SDK source patches + upstream-release link), single atomic commit.
  </done>
</task>

<task type="auto">
  <name>Task 5: Conditional namespace rewrite -- /gsd-X -> /gsd:X in newly-pulled workflows/</name>
  <files>workflows/*.md (only files where a /gsd-X reference was introduced by the sync)</files>
  <action>
    This task is CONDITIONAL -- perform the check first, then act only if needed.

    Detection step (ALWAYS RUN, regardless of skip-or-act outcome -- the artifact is positive proof the check was performed):
      cd "$WT_ROOT" && grep -rEn '/gsd-[a-z]' workflows/ 2>/dev/null | grep -vE '(`/gsd-|gsd-2|gsd-tools|gsd-sdk|gsd-resume-at|gsd-validate|gsd-context-monitor|gsd-phase-boundary|gsd-prompt-guard|gsd-workflow-guard|gsd-read-guard|gsd-read-injection-scanner|gsd-session-state|gsd-statusline|gsd-update-banner|gsd-check-update)' > /tmp/namespace-hits.txt
      wc -l /tmp/namespace-hits.txt

    Surface in SUMMARY.md (the executor's per-task summary should record):
      - The line count of /tmp/namespace-hits.txt
      - A short listing (or "empty file") so the orchestrator can see at a glance that the detection step ran and what it found.

    Interpretation:
      - Each pre-existing exclusion is a legitimate non-skill reference: `bin/gsd-*` binaries, related-project (gsd-2) cross-refs, the gsd-* hook script filenames, gsd-resume-at, etc.
      - Surviving hits in /tmp/namespace-hits.txt are LIVE /gsd-<skill-name> references that upstream's commands/ historically used but the plugin's workflows/ should normalize to `/gsd:<skill-name>` (per project decision from 260420-cns).

    If /tmp/namespace-hits.txt is EMPTY (0 lines): SKIP this task. Record "Task 5 SKIPPED -- /tmp/namespace-hits.txt has 0 lines, no /gsd-X references found in workflows/" as a deviation note in the SUMMARY produced later. Do NOT create an empty commit. The /tmp/namespace-hits.txt artifact (size 0) is the positive proof the skip was deliberate, not forgotten.

    If /tmp/namespace-hits.txt is NON-EMPTY: for each unique file in the hit list, apply the substitution `/gsd-<skill>` -> `/gsd:<skill>` (only at word boundaries -- preserve `bin/gsd-sdk`, `bin/gsd-tools`, hook script names, etc.). Use the same approach as 260420-cns: targeted regex `s|/gsd-([a-z][a-z0-9-]*)|/gsd:\1|g` with a per-file allow-list of exceptions for binary names and hook scripts.

    Post-substitution gate:
      grep -rEn '/gsd-[a-z]' workflows/ 2>/dev/null | grep -vE '(allow-list as above)' should now return ZERO live hits.

    Em-dash diff-scoped gate (only when committing):
      `git -C "$WT_ROOT" diff --cached workflows/ | awk '/^\+/ && !/^\+\+\+/' | grep -c '—'` == 0  (no em-dashes introduced in the normalized workflows)

    Commit (only if substitution was performed; skip if Task 5 was a no-op):
      git -C "$WT_ROOT" add workflows
      git -C "$WT_ROOT" commit -m "fix(260515-f2d): normalize /gsd-X -> /gsd:X in newly-pulled workflows"
  </action>
  <verify>
    <automated>cd "$WT_ROOT" && test -f /tmp/namespace-hits.txt && (! grep -rEn '/gsd-[a-z]' workflows/ 2>/dev/null | grep -vE '(`/gsd-|gsd-2|gsd-tools|gsd-sdk|gsd-resume-at|gsd-validate|gsd-context-monitor|gsd-phase-boundary|gsd-prompt-guard|gsd-workflow-guard|gsd-read-guard|gsd-read-injection-scanner|gsd-session-state|gsd-statusline|gsd-update-banner|gsd-check-update)' | grep -q .)</automated>
  </verify>
  <done>
    /tmp/namespace-hits.txt exists (positive proof the detection step ran). EITHER (a) /tmp/namespace-hits.txt has 0 lines and the task was skipped (no commit, deviation note + line count recorded in SUMMARY) OR (b) /tmp/namespace-hits.txt was non-empty, substitutions applied to one or more workflows/*.md files, and a single atomic commit landed. In either case, the verify command reports zero non-allow-listed /gsd-X references in workflows/.
  </done>
</task>

<task type="auto">
  <name>Task 6: STATE.md -- record Quick Task 260515-f2d row + bump last_activity</name>
  <files>.planning/STATE.md</files>
  <action>
    Append a new row to the "## Quick Tasks Completed" table (insert immediately after the most recent existing row, currently 260513-2zg):

      | 260515-f2d | Sync upstream GSD v1.42.2 + bump plugin to v2.43.0 (agents/, hooks/{context-monitor,workflow-guard}, bin/lib/*, bin/gsd-tools.cjs, sdk/src/*, workflows/, templates/, references/; 4 plugin patches + 2 SDK source patches re-applied; sdk/dist/cli.js rebundled) | 2026-05-15 | <ORCHESTRATOR-TO-FILL> |  | [260515-f2d-new-upstream-version](./quick/260515-f2d-new-upstream-version/) |

    The commit SHA column says "<ORCHESTRATOR-TO-FILL>" because the orchestrator-level merge commit SHA is not known until after the worktree-merge step that runs AFTER this task. The orchestrator will patch the row with the real SHA in the final docs commit (per the established v2.42.6 / 260513-2zg pattern, see SUMMARY commits "5a" and "5b" in that quick task).

    Also update the front-matter:
      - last_updated: "2026-05-15T00:00:00Z"
      - last_activity: 2026-05-15

    And update the "## Current Position" "Last activity:" sentence to describe this sync, e.g.:
      "Last activity: 2026-05-15: Synced upstream GSD v1.42.2 into plugin (v2.42.6 to v2.43.0). Wholesale refresh of agents/, bin/lib/*, bin/gsd-tools.cjs, sdk/src/*, workflows/, templates/, references/ plus 2 hooks (gsd-context-monitor, gsd-workflow-guard). All 4 plugin patches and both SDK source patches preserved. sdk/dist/cli.js rebundled."

    No em-dashes introduced in STATE.md additions (per memory rule).

    Pre-commit grep gates:
      - `grep -c '| 260515-f2d |' .planning/STATE.md` >= 1
      - `grep -c 'last_activity: 2026-05-15' .planning/STATE.md` == 1
      - `grep -c 'last_updated: "2026-05-15T00:00:00Z"' .planning/STATE.md` == 1
      - Em-dash diff-scoped gate: `git -C "$WT_ROOT" diff --cached .planning/STATE.md | awk '/^\+/ && !/^\+\+\+/' | grep -c '—'` == 0  (no em-dashes introduced in this diff)

    Commit (after worktree_branch_check):
      git -C "$WT_ROOT" add .planning/STATE.md
      git -C "$WT_ROOT" commit -m "docs(state): record quick task 260515-f2d (upstream sync v1.42.2 -> plugin v2.43.0)"
  </action>
  <verify>
    <automated>cd "$WT_ROOT" && grep -q '| 260515-f2d |' .planning/STATE.md && grep -q 'last_activity: 2026-05-15' .planning/STATE.md && grep -q 'last_updated: "2026-05-15T00:00:00Z"' .planning/STATE.md</automated>
  </verify>
  <done>
    STATE.md has a new 260515-f2d row in the Quick Tasks Completed table (commit SHA placeholder for orchestrator to patch), front-matter last_updated and last_activity bumped to 2026-05-15, Current Position "Last activity:" sentence reflects the sync, no em-dashes introduced in the diff, single atomic commit.
  </done>
</task>

</tasks>

<verification>

After Task 6 completes, the executor MUST run an end-to-end verification pass:

1. **Commit count check + namespace-hits artifact**: `git -C "$WT_ROOT" log --oneline master..HEAD | wc -l` reports either:
   (a) 5 commits AND `wc -l /tmp/namespace-hits.txt` reports 0 lines (Task 5 was a deliberate skip, no /gsd-X hits in workflows/), OR
   (b) 6 commits AND the 6th commit touches ONLY `workflows/*.md` (Task 5 ran a real substitution).
   Any other combination (e.g. 5 commits but /tmp/namespace-hits.txt missing or non-zero, 6 commits but the extra one touches non-workflows files) is a deviation and must be surfaced.

2. **Patch markers survive**: grep all in-source patch markers across the worktree tree:
   - `grep -c '\[PLUGIN PATCH\]' bin/lib/core.cjs` >= 2
   - `grep -q '#PLUGIN-MODEL-CATALOG-PATH' bin/lib/model-catalog.cjs`
   - `grep -q '#PLUGIN-HOOK-CONTEXT-MONITOR' hooks/gsd-context-monitor.js`
   - `grep -q '\[PLUGIN PATCH\]' sdk/src/query/state-project-load.ts`
   - `grep -q '\[PLUGIN PATCH\]' sdk/src/query-gsd-tools-path.ts`
   All present.

3. **Plugin-owned wrappers untouched**: sha256 of bin/gsd-sdk and bin/gsd-sdk.cmd unchanged vs master baseline.

4. **No commands/ dir**: `test ! -d "$WT_ROOT/commands"` passes.

5. **SDK bundle rebuilt + carries both patches**: `[ $(stat -f%z "$WT_ROOT/sdk/dist/cli.js" 2>/dev/null || stat -c%s "$WT_ROOT/sdk/dist/cli.js") -ge 1400000 ]` AND `grep -c CLAUDE_PLUGIN_ROOT "$WT_ROOT/sdk/dist/cli.js"` >= 2.

6. **Regression test trifecta**: `node tests/mcp-stdio-framing.test.cjs && node tests/workspace-json-integration.test.cjs && node tests/hooks-smoke.test.cjs` all pass.

7. **Version consistency**: `grep -c '"version": "2.43.0"'` across package.json, .claude-plugin/plugin.json, .claude-plugin/marketplace.json sums to >= 3; no surviving 2.42.6 strings in those three files.

8. **CHANGELOG well-formed**: `[2.43.0]` section exists, ordered above `[2.42.6]`, dated 2026-05-15, references "upstream GSD 1.42.2".

9. **README header refreshed**: `grep -q 'Based on:.*GSD 1.42.2' <(head -10 README.md)` AND `! grep -q 'GSD 1.41.2' <(head -10 README.md)`.

10. **Worktree clean**: `git -C "$WT_ROOT" status --porcelain` returns empty (nothing uncommitted).

</verification>

<success_criteria>

- All 5 or 6 atomic commits landed on the worktree branch with clear conventional-commit messages.
- All 4 active plugin patches (in-tree) AND 2 SDK source patches present and grep-able post-sync.
- 4 dispatch cases (migrate, write-phase-memory, checkpoint, hook) intact in bin/gsd-tools.cjs.
- Plugin-owned wrappers (bin/gsd-sdk, bin/gsd-sdk.cmd) byte-identical to baseline.
- commands/ remains absent.
- sdk/dist/cli.js rebuilt, >= 1.4 MB, contains >= 2 CLAUDE_PLUGIN_ROOT matches from SDK source patches.
- 3 regression test suites pass (mcp-stdio-framing, workspace-json-integration, hooks-smoke).
- All three metadata files at v2.43.0.
- README.md header (first 10 lines) references upstream GSD 1.42.2, plugin v2.43.0; stale v1.41.2 / 2.42.6 references removed from header.
- CHANGELOG.md has well-formed [2.43.0] entry.
- STATE.md updated with new row + bumped dates.
- No em-dashes introduced in newly-added content (diff-scoped gates clean for README, CHANGELOG, STATE, workflows).
- /tmp/namespace-hits.txt exists as positive proof of the Task 5 detection step.
- Worktree clean, ready for orchestrator merge + tag + release.

</success_criteria>

<output>
After completion, the orchestrator will:
  - Merge the worktree branch into master (--no-ff)
  - Patch the STATE.md row with the real merge commit SHA (final docs commit)
  - Produce `.planning/quick/260515-f2d-new-upstream-version/260515-f2d-SUMMARY.md` mirroring the 260513-2zg-SUMMARY.md structure

The executor's responsibility ends at Task 6. The orchestrator handles merge + docs-fixup + release-tag.
</output>
</content>
</invoke>